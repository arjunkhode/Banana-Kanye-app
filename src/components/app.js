import React, { Component } from 'react';
// Use {this.props.children} to include components inside App Route

export default class App extends Component {
  render() {
    return (
      <div>React simple starter</div>
    );
  }
}
